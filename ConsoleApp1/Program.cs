using Labs.DataAccess.Connection;
using Labs.DataAccess.Helpers;
using Labs.DataAccess.Models;
using Labs.DataAccess.Repositories;

namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //var repo = new LinqDestinationRepository();

            //var result = repo.Get(1);

            

            ////var create = repo.Create(new Destinations()
            ////{
            ////    DestinationName = "Test123"
            ////});

            //var results = repo.GetAll();

            //var update = repo.Update(new AircraftTypes()
            //{
            //    Id = results.Last().Id,
            //    DestinationName = "Test1234"
            //});

            //results = repo.GetAll();

            //var delete = repo.Delete(results.First(x => x.DestinationName.Equals("Test1234")).Id);

            //results = repo.GetAll();
        }
    }
}